info:
  name: livez
  type: http
  seq: 2

http:
  method: GET
  url: "{{order_url}}/livez"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: This request checks whether the Order Service process is running.
