info:
  name: readyz
  type: http
  seq: 3

http:
  method: GET
  url: "{{order_url}}/readyz"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: This request retrieves a list of users from the JSONPlaceholder API.
