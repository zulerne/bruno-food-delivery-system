info:
  name: Get All Orders
  type: http
  seq: 6

http:
  method: GET
  url: "{{order_url}}/orders"
  headers:
    - name: X-Customer-ID
      value: test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
