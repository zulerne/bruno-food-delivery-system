info:
  name: Cancel Order
  type: http
  seq: 7

http:
  method: POST
  url: "{{order_url}}/orders/{{item_id}}/cancel"
  headers:
    - name: X-Customer-ID
      value: test
  auth: inherit

runtime:
  variables:
    - name: item_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
