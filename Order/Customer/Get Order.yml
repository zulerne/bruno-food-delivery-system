info:
  name: Get Order
  type: http
  seq: 5

http:
  method: GET
  url: "{{order_url}}/orders/{{item_id}}"
  headers:
    - name: X-Customer-ID
      value: test
  auth: inherit

runtime:
  variables:
    - name: item_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
